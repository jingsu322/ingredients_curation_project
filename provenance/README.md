# provenance/

- `source_manifest.csv`: Machine-readable listing of raw/sample files (path, bytes, sha256, mtime_iso).
- `runs/`: Run-time metadata (e.g., `run_meta.json`) for each pipeline execution.
- `checksums.txt`: Hashes of key deliverables for integrity verification.
